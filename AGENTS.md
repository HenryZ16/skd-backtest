# 项目规则

适用于在本仓库工作的所有 agent。

1. `D:\skd-backtest\BACKTEST_PLATFORM_SPEC_v2.md` 是只读需求文档。Agent 永远不得修改、覆盖、删除或重命名该文件，包括格式、编码和换行调整；只能读取和引用。
2. Agent 维护的文档必须直接反映项目当前状态。更新时在原文对应位置修改、添加或删除内容，不得在文末或其他位置追加“某年某月某日更新”、补充更正或其他叠层说明来保留已经过时的表述。例如，开发阶段变化后，应直接改写 `README.md` 中“当前版本是可安装、可调用的流程骨架”的描述。历史变更由 Git 记录。
3. 每次完成修改和验证后，将本次改动设置为 staged changes，供用户在 VS Code 中审阅，无需在对话中展示修改内容。必须取得用户针对本次暂存改动的明确同意后才能提交；未经同意不得执行 Git 提交，包括 `git commit --amend`。此前的提交许可不自动授权后续提交；确认后的改动若发生变化，须重新暂存并等待用户确认。每次 Git 提交完成后，必须将本地提交同步到对应远端分支，并确认同步成功；若同步失败，必须明确告知原因和未同步状态。
4. 每次完成设计、准备修改代码前，必须先对照 `BACKTEST_PLATFORM_SPEC_v2.md` 复核本次方案。若方案存在不符合需求文档的地方，必须退回重新设计，并在重新复核通过后才能修改代码；不得先实现再补解释。需求文档未提及的事项不作为一致性检查的限制，不必因此额外请求确认。
5. 每次发布 GitHub Release，必须提供可由 pip 直接安装的 Python wheel（.whl）附件，不能仅提供 GitHub 自动生成的源码压缩包。发布前须验证安装和基本运行，发布说明须同时包含 pip 和 uv 的对应安装命令。
6. GitHub Release 标题只能包含版本号，例如 `0.1.0-beta.2`，不得附加“首个可用 beta 版本”等描述文字。
7. 发布前必须确认对应提交以及 wheel、源码分发包包含项目的开源许可证。
