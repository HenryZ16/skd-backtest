# 总设计独立一致性审查

## 结论

| 审查问题 | 最终结论 |
|---|---|
| 总设计文档要求是否全部实现？ | **是** |
| 是否存在实现与总设计文档已写明内容相悖？ | **否** |

结论覆盖只读 [BACKTEST_PLATFORM_SPEC_v2.md](../BACKTEST_PLATFORM_SPEC_v2.md) 的全部明示功能，包括第 608–610 行明确列出的后续正式优化器要求；不以“首版完成”替代完整交付判定。首轮识别的四项缺口均已修复并通过独立复核，没有尚待返修的已确认需求缺失或冲突。

审查由与实现主任务分离的独立审查 agent 完成。先逐段阅读总设计、追踪生产代码与测试，再检查返修源码和现行说明；测试通过是佐证，不代替逐条需求判定。

## 判定边界与用户指令

- 用户明确要求“完整删除公司行为组件相关的内容，空阶段也不要留”，覆盖总设计第 153–175、193、558、597、628 行涉及公司行为的内容。这些条款不要求恢复；实现中没有公司行为组件、主题或空阶段。
- 明确首版要求、明确后续要求及未限定阶段要求全部纳入覆盖关系。后续功能不能仅以当前阶段说明豁免。
- 总设计未指定的接口搬运形式、具体优化目标、协方差估计方案、求解器、换手范数、缺失指标处理和命令配置文件格式，不因采用某种实现选择而判为冲突。
- Long Only 的配置与现货执行能力检查已实现；long_only=False 明确拒绝。规格没有明示融券、借券、负股数会计或空头执行，不能由该字段自行扩张出必须交付卖空业务的要求。满仓、单股、主动权重、行业、风格和换手约束均有实际实现。
- 财务因子与外部成分、基准、风险估计必须由数据生产方提供当时已知的 PIT 版本。规格第 123 行要求提前处理财务数据；平台按日期隔离已处理数据，不承担总设计未要求的原始财报重建器。测试不证明任何未经审计的真实数据源已经满足 PIT。
- 可复现性在固定提交、数据、配置及数值环境下验收。平台控制标准提交的每次评测模型生命周期及 Python/NumPy 传统全局随机流。参赛者自行创建的随机生成器、外部熵和非确定性硬件算法须遵守提交可复现约定；直接传 callable 时，其自有对象状态由调用者管理。此边界已明确写入使用说明，不宣称可把任意非确定性参赛代码自动改造成确定性代码。
- 不额外要求规格未明示的恶意代码操作系统沙箱、任意训练调用动态拦截器、排行榜 UI 或特定厂商完整风险产品。平台不训练模型、不向推理提供隐藏标签，Metrics 不写死 QR 的排行榜混合权重。
- examples/basic_usage.py 保持原状；模型异步默认启用；运行缓存发布和读取共享引用，固定表结构每次运行仅首次检查。研究输入隔离所需副本、确实改变的表以及小型日志明细副本不属于无目的克隆。

## 完整需求覆盖矩阵

下列条文行号均指只读总设计；实现位置相对于 src/skd_backtest/。每行的“通过”均指对应功能已追踪到实际执行路径，建议项和输入前提另行说明。

| 总设计条文 | 阶段/性质 | 覆盖结果与生产实现证据 |
|---|---|---|
| 11–27：预测、组合构建、交易规则、费用、核算、评价分工 | 未限定 | 通过。engine.py 编排独立 Runner、Optimizer、Broker、CostModel、Accounting、Evaluator、Metrics、Writer；金融责任没有交给参赛模型。 |
| 33–44：标准提交目录，inference.py 与多个 model/ 文件，训练参数固化 | 未限定 | 通过。submission_runner.py:54–90 加载标准提交、传入完整 model_dir，仅实例化并调用推理，不执行平台训练。 |
| 48–80：InferenceModel(model_dir) 初始化一次，逐信号日 predict | 未限定 | 通过。submission_runner.py:78–82 创建单次评测实例；engine.py:85–97 管理重复评测重建；inference_pipeline.py 单 worker 按日期调用同一实例。 |
| 83–87：参赛者自行完成数据处理与特征工程 | 未限定 | 通过。Runner 只传 as_of_date 与研究数据、接收分数，不理解或重新拟合队伍的特征处理。 |
| 89–97：日期、合法池唯一全覆盖、有限分数 | 未限定 | 通过。submission_runner.py:107–117 首次检查结构、每批检查日期/股票池/数值并排序。 |
| 112–121：按历史顺序，t 日只能收到截至 t 的可用信息 | 未限定 | 通过。data_provider.py:343–363 对研究数据切片并隔离；inference_pipeline.py 顺序推理；交易字段、账户和未来标签不传给模型。 |
| 123：财务数据提前 PIT | 输入前提 | 通过接口责任核查。消费上游已处理的日频因子，按日期裁剪；docs/usage.md 明确披露日 PIT 的生产方责任，不以日期过滤冒充财报版本重建。 |
| 129–145：研究价/执行价分离，真实 OHLC 或因子，缺数据时退化 | 未限定 | 通过。data_provider.py:64–160 支持 raw OHLC 或 adjusted/factor；研究输入保持后复权；config.py 拒绝缺能力的真实股数模式，adjusted_return 使用资产金额记账。 |
| 147–149：历史限价、停牌及状态建议字段 | 建议 | 已实现直接读取历史 upper_limit/lower_limit 与停牌；不按 ST、板块或固定 10% 比例推造限价。 |
| 153–175：公司行为处理 | 用户覆盖 | 已按明确指令移除，不要求恢复。 |
| 181–189：现金、总股数、可卖股数、市值、权益 | 未限定 | 通过。contracts.py:AccountState、Broker 的锁定批次及 Accounting 的逐日快照保存所需状态。 |
| 191–195：日初→开盘→卖出→现金→买入→持仓→收盘 | 未限定 | 通过。engine.py 固定阶段推进；broker.py:403–522 先卖后买。公司行为阶段按用户指令排除。 |
| 197：不融资、不杠杆、现金非负 | 未限定 | 通过。broker.py 校验目标总权重和非负目标；先结算卖出现金，再按真实现金缩减买入和重新计费。 |
| 203–214：底层 T+1 | 未限定 | 通过。broker.py:71–118 只释放到期锁定；买入仅增加 total_shares 并记录下一交易日解锁日期；卖出受 sellable_shares 限制。 |
| 216–228：100 股买入与剩余零股清仓 | 未限定 | 通过。broker.py:258–284,410–416,458–477 实现整手买入与可卖余股处理；零股专项验收通过。 |
| 232–244：历史限价、涨停禁买、跌停禁卖 | 未限定 | 通过。data_provider.py 读取当日绝对限价；broker.py:206–224,294–301 依据 raw_open 与方向判定。 |
| 246：开盘成交不得使用当日 high/low/close | 未限定 | 通过。开盘市场包只包含开盘必要字段与严格早于当日的参考价；Broker 没有收盘价输入。 |
| 250–258：停牌/缺开盘拒单、保留持仓、沿用历史有效价 | 未限定 | 通过。Broker 拒绝买卖但保留持仓；data_provider.py:33–61,163–180 排除停牌或状态未知的历史报价；Accounting 选择当前或历史可用价。连续停牌与跨批次/seed 已验收。 |
| 262–278：历史 CSI300 成分/权重、调出目标零、目标不等于实际 | 未限定 | 通过。每日 Barra 成员行表示合法池；ReferenceData 读取当天真实权重；portfolio_optimizer.py 对退出持仓输出零目标；未成交持仓继续保留。 |
| 284–290：截面排名与统一 alpha，避免原 score 尺度支配 | 未限定 | 通过。Top-K 按排名；tilt 和 Barra 使用平均并列百分位；严格单调分数变换验收通过。 |
| 292–302、566–568：Optimizer 与 Broker 独立 | 未限定 | 通过。Optimizer 只生成 TargetPlan；Broker 独立决定可执行股数、成交和现金，不导入风险优化算法。 |
| 304–310：Long Only、Fully Invested、单股约束配置 | 未限定 | 通过现货能力及约束核查。config.py、portfolio_optimizer.py、risk_optimizer.py 实际处理配置，满仓不可行明确失败；不把 long_only=False 解释成隐含空头业务。 |
| 311–314：主动权重、行业、风格、换手约束 | 明确后续 | 通过。risk_optimizer.py:78–134 对主动权重设置上下界，对行业/风格主动暴露设置限制，并将退池股票归零计入双边 L1 换手。 |
| 316：Top-K 或简单 benchmark tilt | 明确首版 | 通过。portfolio_optimizer.py 保留两种简单算法、并列排序、单股上限与现金余量。 |
| 316、608–610：Barra 风险模型与正式优化器 | 明确后续 | 通过。risk_optimizer.py:28–53 实际构建 XFXᵀ+diag(D)；56–195 求解排名 alpha 与主动风险目标，并复核全部约束。风险参数来自当日 PIT 外部来源。 |
| 330–342：权益×目标权重÷真实开盘价、整手、卖出释放现金后再买 | 未限定 | 通过。Broker 按开盘快照权益计算目标股数，按实际成功卖出所得现金与费用逐笔缩减买入，不使用理论卖出现金。 |
| 350–367：t 收盘信号、下一交易日开盘、可配置滑点 | 未限定 | 通过。RunCalendar 固定下一交易日；Engine 收盘后发布信号；CostModel 以 raw_open×(1±slippage) 计价。 |
| 373–394：独立成本、佣金、历史印花税/过户费、日期生效 | 未限定 | 通过。cost_model.py:16–53,55–139 建立历史费率表，按最新已生效日期取费率；非空表未覆盖日期会失败。 |
| 400–429：逐日现金/股数/市值/权益/NAV/基准与主动收益 | 未限定 | 通过。accounting.py:192–285 使用真实收盘价或有效历史价估值；复合基准 NAV；active_return=portfolio_return-benchmark_return。 |
| 436–451：预测独立评价，H 交易日 next-open 标签 | 未限定 | 通过。label_provider.py 按全市场交易日定位 t+1 和 t+H+1，独立读取必要未来端点，不污染推理播放状态。 |
| 453–457：每个调仓日 Spearman RankIC | 未限定 | 通过。prediction_evaluator.py:25–42 对有效分数/标签采用平均并列排名后相关，保留各信号日记录。 |
| 459–468：四项预测指标，底层 RankICIR 不固定年化 | 未限定 | 通过。metrics.py:112–122,179–183 输出 Mean RankIC、Std、IR、正值比例，原始 IR 不乘固定年化系数。 |
| 474–488：11 项组合指标 | 未限定 | 通过。metrics.py:124–194 输出收益、年化、超额、波动率、回撤、Tracking Error、Information Ratio、Sharpe、Turnover、费用、失败订单。 |
| 493–503：active_return、Tracking Error、Information Ratio | 未限定 | 通过。Accounting 计算逐日主动收益；Metrics 按标准差/均值公式计算，年化交易日数默认 252。 |
| 506–508：QR 配置赛制，Metrics 不固定排行榜权重 | 未限定 | 通过。Metrics 仅输出原始指标；没有硬编码最终榜单组合权重。 |
| 514–537：metrics.json、七 CSV、run.log 及关键字段 | 未限定 | 通过。schemas.py 固定审计列；result_writer.py:24–118 实际生成九个文件；统一评测入口保证有输出目录。 |
| 539–545：拒单原因与完整审计链 | 未限定 | 通过。订单/成交包含 signal_date、order_id 关联信息；从分数、目标、订单、成交、持仓可追踪至每日 NAV。 |
| 551–562：各模块边界 | 未限定 | 通过。SubmissionRunner、DataProvider、Evaluator、Optimizer、Broker、CostModel、Accounting、Metrics、Writer 各司其职；公司行为行按用户覆盖排除。 |
| 574–580：统一运行命令示例 | 建议 | 已提供 evaluate.py、包内入口及 skd-evaluate；采用 JSON/TOML。规格的 YAML 命令是建议，不要求特定文件格式。 |
| 584：完整统一运行链 | 未限定 | 通过。标准加载→一次初始化→交易日历→截至当日输入→推理与校验→标签/预测评价→优化→下一开盘→现金持仓→日估值→指标→文件均有实际路径；标签在事后评价阶段计算，避免暴露未来信息。 |
| 592–597：无未来信息、信号执行分离、历史规则、目标实际分离、价格分离 | 未限定 | 通过。对应数据包、日历、Broker、CostModel 和 Accounting 行为均已核查；公司行为原则按用户指令排除。 |
| 598：同提交/数据/配置可复现 | 未限定 | 通过标准评测生命周期及声明的数值环境/提交契约验收。random_seed 默认 0；加载和推理使用 Runner 私有 Python/NumPy 随机流；标准提交重复评测重建模型。 |
| 604–606：首版完整业务链 | 明确首版 | 通过。真实组件联调覆盖预测、标签、两种价格模式、交易规则、账户、日 NAV、指标及输出。 |
| 612：替换 Optimizer 不修改 Broker/Accounting/Metrics | 未限定 | 通过。新增 Barra 方法仍发布相同 TargetPlan；原交易、会计和指标组件直接完成端到端运行。 |
| 618–640：开发前数据确认与模式退化 | 输入前提 | 通过。DataCapabilities、ReferenceSources 与源适配器验证配置/来源，严格股数模式要求真实价格或因子；缺失时使用明确的 adjusted_return 模式，不伪造原价。 |
| 646–652：统一规则完成各队伍私榜评测 | 最终 | 通过。标准提交和统一配置入口覆盖完整链，原始指标及审计结果可供 QR 的榜单层使用；不把建议或输入前提扩展成未写明的产品功能。 |

## 四项返修的最终证据

| 复核项 | 最终实现 | 最小验收证据 |
|---|---|---|
| 正式 Barra 风险优化与高级约束 | risk_optimizer.py:28–195；portfolio_optimizer.py:44–53；ReferenceData 提供当天 F/D，PortfolioInputs 扩展风险数据但保持目标协议 | 手算 Σ 与最优权重，各约束单独/联合生效、现金余量、退池换手、不可行输入；真实 Engine 使用原 Broker/Accounting/Metrics 成功完成。 |
| 标准提交加载 | submission_runner.py:54–90；engine.py:62–69,85–97；包内 evaluate.py 和根入口 | 多模型文件及相对辅助导入、每次评测初始化一次、按日期多次推理、重复评测创建新模型、CLI 生成全部九个文件。 |
| 停牌报价进入历史估值链 | data_provider.py:39–41 的有效价格同时要求明确非停牌；163–180 的 seed 同样过滤 | 两种价格模式，正常 10→停牌 99→仍停牌 NaN：历史参考价维持 10；跨批次、seed 和实际 NAV 全链验证通过。 |
| 随机推理与模型运行状态复现 | config.py:random_seed；submission_runner.py:22–39,76–82,101–105；engine.py 的每次运行生命周期 | 模块加载、模型构造与 predict 都消耗 Python/NumPy 随机值的模型，在重复运行和同步/异步组合中七表、账户、指标一致；宿主 RNG 恢复；更换种子会改变分数。 |

## 独立执行的验证

1. 在冻结源码上独立执行 .venv/Scripts/python.exe -B -m unittest discover -s tests -v：**119 项全部通过**。覆盖真实组件、接口协议、错误与清理、两种价格体系、标准提交、正式优化、输出与重入；未以测试数量替代需求追踪。
2. 另独立构造 **81 个可行组合**：主动权重限制取未设置/0/0.1，行业限制取未设置/0/0.05，风格限制取未设置/0/0.1，换手限制取未设置/0/0.2。两股风险模型的预期最优 A 权重由手算限制得出，全部匹配；精确中性和零换手没有假不可行。
3. 另用独立解析投影对照 **4、30、100、300 股** 的满仓、单股上限、排名风险优化。对角资产风险模型下，解析解通过一维预算乘子二分得到；生产优化器与其最大逐股误差如下：

| 股票数 | 风险厌恶系数 | 单股上限 | 最大逐股误差 |
|---:|---:|---:|---:|
| 4 | 2 | 0.40 | 1.05×10⁻¹⁶ |
| 30 | 20 | 0.06 | 4.24×10⁻⁹ |
| 100 | 100 | 0.02 | 3.02×10⁻⁸ |
| 300 | 500 | 0.01 | 4.01×10⁻¹⁰ |

该解析对照评估解的数值精度；所有预算和单股约束均满足，没有把误差上界冒充求解器对任意问题的全局保证。

4. 检查总设计与 examples/basic_usage.py 相对 HEAD 无差异；检索 src、tests、docs、README、examples 和打包声明，确认没有公司行为实现或空阶段残留。
5. 核对 README、docs/usage.md、docs/design.md、docs/interfaces.md：正式能力、协议版本 3、随机源责任、必要风险输入、约束定义、可选 SciPy 安装及源码包入口同步一致。历史性能报告明确对应初验快照，不冒充当前返修后的重新测量值。
6. git diff --check 未发现空白错误。本审查只写本报告，不暂存、不提交，也不修改总设计、示例或其他源码文档。

## 审查快照

- 总设计 SHA256：4D0575A88E4FA245B05C9D198BE42C6515780FD8CF8CE44B16896F87D89C1F1D
- examples/basic_usage.py SHA256：2F24A684BBF0A4B6A7945054751167DECE9B822C79C87663586635EB1516BF5C
- src/skd_backtest 下 20 个 Python 文件聚合 SHA256：4FAA4C1C44EE15F37B8C91B0E2714A7388C3A47F97BC38C76BFFBE60DBA2690C

聚合指纹按文件名排序；每个文件依次写入相对仓库的 POSIX 路径 UTF-8 字节长度（8 字节大端）、路径字节、文件字节长度（8 字节大端）和原始文件字节后计算 SHA256。该指纹标识本次审查的源码内容，不代表任何未运行的环境或真实数据源认证。
